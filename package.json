{
  "name": "getir-backend-assignment",
  "version": "0.1.0",
  "description": "api with one endpoint in order to fetch data with POST request",
  "main": "dist/index.js",
  "scripts": {
    "prestart": "bash prestart.sh",
    "start": "node ./dist/bin/www.js",
    "test": "jest",
    "pretest": "bash tests/pretest.sh"
  },
  "keywords": [
    "api",
    "express",
    "rest"
  ],
  "author": "keskinsaf",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.8.9"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.2",
    "@types/jest": "^24.9.0",
    "@types/mongoose": "^5.5.41",
    "@types/supertest": "^2.0.8",
    "jest": "^24.9.0",
    "supertest": "^4.0.2"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  }
}
